You are running HEADLESSLY inside the candidate's resume repo. Task: write (or rewrite) ONLY the cold outreach email for the job below. A tailored CV already exists; do not touch any .tex or .pdf files.

## Job

- Title: {TITLE}
- Company: {COMPANY}
- Location: {LOCATION}
- URL: {URL}

### Full job description

{DESCRIPTION}

## Instructions

1. Read `profile.md` for the candidate's real facts. If `{APP_DIR}/notes.md` exists, read it to see what the tailored CV emphasized; keep the email consistent with it. Never fabricate anything.
2. Overwrite `{APP_DIR}/cold_email.md` with:
   - A metadata block: `To: [find - recruiter or hiring manager at {COMPANY}]`, a suggested subject line, and the job URL.
   - Then the email body following the style guide below.
3. Your FINAL message must be exactly:
EMAIL: {APP_DIR}/cold_email.md
STATUS: done

{EMAIL_STYLE}
